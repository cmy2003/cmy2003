# dsh-plugin-k8s-sdk 使用手册

## 1. 包信息

| 项目 | 内容 |
| --- | --- |
| 包名 | dsh-plugin-k8s-sdk |
| 版本 | 0.1.0 |
| 类型 | DSH / Cordis 插件 |
| 运行时 | Node.js >= 18 |
| K8s 客户端 | @kubernetes/client-node |
| 打包文件 | dsh-plugin-k8s-sdk-0.1.0.tgz |
| 开源协议 | MIT |

---

## 2. 包内文件说明

```text
dsh-plugin-k8s-sdk-0.1.0.tgz
├── dist/
│   ├── index.js
│   ├── index.d.ts
│   ├── self-test.js
│   └── source map 文件
├── cordis.patch.yml
├── package.json
├── README.md
├── LICENSE
└── 使用手册.md（本手册）
```

---

## 3. 功能说明

本插件提供三个 DSH Agent 工具：

| 工具名 | 作用 | 参数 |
| --- | --- | --- |
| k8s_list_nodes | 获取集群所有节点名称、内网 IP、Ready 状态 | 无 |
| k8s_list_pods | 获取指定命名空间下的 Pod 名称和 Phase | namespace，默认 default |
| k8s_get_pod_log | 获取指定 Pod 最新日志 | podName、namespace、tailLines |

---

## 4. 环境要求

- Node.js >= 18
- DSH >= 1.0.0
- 可访问的 Kubernetes 集群
- 有效的 kubeconfig 文件

---

## 5. 安装方法

### 5.1 从 npm 包安装

```bash
npm install -g dsh-plugin-k8s-sdk-0.1.0.tgz
```

### 5.2 从 GitHub Release 安装

将 `dsh-plugin-k8s-sdk-0.1.0.tgz` 上传到 GitHub Release，然后执行：

```bash
dsh plugin --profile web add github:你的GitHub用户名/dsh-plugin-k8s-sdk
```

### 5.3 本地目录安装

```bash
dsh plugin --profile web add local:D:/DSH/dsh-plugin-k8s-sdk
```

---

## 6. 前置配置

### 6.1 准备 kubeconfig

默认读取位置：

```text
Windows: C:\Users\你的用户名\.kube\config
Linux/macOS: ~/.kube/config
```

也可以设置环境变量：

```bash
set KUBECONFIG=D:\kube\config
```

### 6.2 校验 kubeconfig

```bash
npm run self-test
```

成功返回：

```json
{
  "success": true,
  "currentContext": "kubernetes-admin@kubernetes",
  "nodeCount": 4,
  "nodes": [
    {
      "name": "master1.cmy.org",
      "internalIP": "192.168.31.101",
      "ready": true
    }
  ]
}
```

---

## 7. VMware NAT 环境说明

如果 DSH 运行在宿主机，K8s 集群在 VMware 虚拟机中：

1. 配置 VMware NAT 端口转发；
2. 将宿主机端口映射到虚拟机 APIServer 的 6443；
3. 修改 kubeconfig 中 server 地址为宿主机可达地址。

示例：

```yaml
server: https://127.0.0.1:16443
```

或：

```yaml
server: https://192.168.1.100:16443
```

---

## 8. DSH 中加载插件

```bash
dsh plugin --profile web add github:你的GitHub用户名/dsh-plugin-k8s-sdk
```

查看插件：

```bash
dsh plugin --profile web list
```

临时关闭：

```bash
dsh plugin --profile web disable dsh-plugin-k8s-sdk
```

临时开启：

```bash
dsh plugin --profile web enable dsh-plugin-k8s-sdk
```

---

## 9. DSH WebUI 调用示例

在 DSH WebUI 中直接输入：

- 查看所有节点名称、内网 IP 和 Ready 状态
- 列出 default 命名空间下所有 Pod
- 查看 nginx-deployment-xxxxx 这个 Pod 最近 100 行日志
- 查看 kube-system 命名空间下 coredns-xxxxx 的日志，最近 200 行

---

## 10. 工具返回格式

所有工具均返回 JSON 字符串：

### 成功格式

```json
{
  "success": true,
  "data": {}
}
```

### 失败格式

```json
{
  "success": false,
  "error": {
    "code": "错误码",
    "message": "中文错误描述"
  }
}
```

---

## 11. 错误码说明

| 错误码前缀 | 含义 |
| --- | --- |
| KCFG_* | kubeconfig 不存在、格式错误、无上下文或未初始化 |
| NET_* | APIServer 网络不通、超时、DNS 解析失败 |
| AUTH_* | 401 认证失败或 403 权限不足 |
| API_* | 404 或其他 API 业务错误 |

---

## 12. 自检命令

```bash
npm run self-test
```

自检内容：

1. 加载 kubeconfig；
2. 创建 K8s 客户端；
3. 调用 listNode()；
4. 输出节点诊断 JSON。

---

## 13. 常见问题

### 13.1 NET_CONNECTION_REFUSED

原因：

- kubeconfig 缺失，SDK 回退到 localhost:8080；
- 或 APIServer 地址不可达。

解决：

- 配置 kubeconfig；
- 检查 APIServer 地址和端口；
- 检查 VMware NAT 或防火墙。

### 13.2 NET_DNS_NOT_FOUND

原因：

- kubeconfig 中的 server 域名无法解析。

解决：

- 修改 hosts 文件；
- 或把 server 改为 IP。

### 13.3 AUTH_UNAUTHORIZED / AUTH_FORBIDDEN

原因：

- token 或证书无效；
- RBAC 权限不足。

解决：

- 更新 kubeconfig；
- 使用具有权限的 ServiceAccount。

### 13.4 节点 Ready 为 false

原因：

- kubelet 异常；
- CNI 网络异常；
- 节点资源压力；
- 证书过期。

排查：

```bash
kubectl describe node <节点名>
```

## 15. MIT 协议

本项目使用 MIT License，允许自由使用、修改、分发和商用。
